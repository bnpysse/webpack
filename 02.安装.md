# webpack的安装

- 安装本地的webpack
- 需要安装两个东东：webpack webpack-cli

## npm的安装

`npm`是第三方管理器，主要是为了更好地对`JavaScript`的模块库进行管理，当前的前端开发已经跟过去的概念大不相同，复杂得多。所以只能借助一个`包管理器`来完成这些工作，另外还有其它的包管理器选择，比如：`yarn/bower`，etc...

- `npm install xxx`:安装某个模块
- `npm install xxx -g`:全局安装某个模块
